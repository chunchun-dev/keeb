import React from 'react'

function SwitchPreview({title, type, slug}) {
    return (
        <div>
            <h1>{title}</h1>
            <p>{type}</p>
        </div>
    )
}

export default SwitchPreview
